# Core
numpy>=1.24.0
scipy>=1.10.0
torch>=2.0.0
torchaudio>=2.0.0

# Audio
soundfile>=0.12.0
sounddevice>=0.4.6

# VAD
# silero-vad (installed via torch.hub)

# ASR
# sherpa-onnx - for ONNX ASR (recommended, CPU-friendly)
# pip install sherpa-onnx  # Optional: Add to path

# LLM
openai>=1.0.0

# TTS - Multiple options
edge-tts>=6.1.0             # Online fallback (free, no API key)
# vieneu>=0.1.0             # VieNeu-TTS (CPU-friendly, offline) - recommended for limited hardware
# qwen_tts>=0.1.0           # Qwen-TTS (GPU, high quality, voice cloning)

# API Server
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
websockets>=11.0

# Utilities
python-dotenv>=1.0.0
nest-asyncio>=1.5.0         # For nested event loops

# Development (optional)
# pytest>=7.0.0
# black>=23.0.0
# isort>=5.12.0
# mypy>=1.0.0
